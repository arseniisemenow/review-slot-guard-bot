openapi: 3.0.0
info:
  title: Review Slot Guard Bot Webhook
  version: 1.0.0
paths:
  /webhook:
    post:
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: ${function_id}
      operationId: webhook
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: OK
